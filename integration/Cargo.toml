[package]
name = "integration"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
integration-lib = { path = "integration-lib" }
actix-web = { version = "4.0.0-beta.8" }
env_logger = "*"
